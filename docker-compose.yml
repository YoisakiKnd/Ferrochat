services:
  ferrochat:
    image: ghcr.io/yoisakiknd/ferrochat:${FERROCHAT_TAG:-latest}
    ports:
      - "3000:8080"
    volumes:
      - ferrochat-data:/data
    restart: unless-stopped

volumes:
  ferrochat-data:
