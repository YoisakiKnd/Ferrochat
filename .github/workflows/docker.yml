name: docker
on:
  push:
    tags: ["v*"]
jobs:
  build:
    strategy:
      matrix:
        include:
          - platform: linux/amd64
            runner: ubuntu-latest
          - platform: linux/arm64
            runner: ubuntu-24.04-arm
    runs-on: ${{ matrix.runner }}
    permissions:
      contents: read
      packages: write
    steps:
      - uses: actions/checkout@v4
      - id: image
        run: echo "name=$(echo "ghcr.io/${GITHUB_REPOSITORY}" | tr '[:upper:]' '[:lower:]')" >> "$GITHUB_OUTPUT"
      - uses: docker/setup-buildx-action@v3
      - uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}
      - id: meta
        uses: docker/metadata-action@v5
        with:
          images: ${{ steps.image.outputs.name }}
          tags: |
            type=semver,pattern={{version}}
            type=semver,pattern={{major}}.{{minor}}
            type=raw,value=latest
      - id: build
        uses: docker/build-push-action@v6
        with:
          context: .
          file: docker/Dockerfile
          target: slim
          platforms: ${{ matrix.platform }}
          outputs: type=image,name=${{ steps.image.outputs.name }},push-by-digest=true,name-canonical=true,push=true
          cache-from: type=gha
          cache-to: type=gha,mode=max
          labels: ${{ steps.meta.outputs.labels }}
          provenance: false
          sbom: true
      - name: export digest
        run: mkdir -p /tmp/digests && printf '%s\n' "$DIGEST" > "/tmp/digests/${PLATFORM//\//-}"
        env:
          DIGEST: ${{ steps.build.outputs.digest }}
          PLATFORM: ${{ matrix.platform }}
      - uses: actions/upload-artifact@v4
        with:
          name: digest-slim-${{ strategy.job-index }}
          path: /tmp/digests/*
      - id: meta-full
        uses: docker/metadata-action@v5
        with:
          images: ${{ steps.image.outputs.name }}
          tags: |
            type=semver,pattern={{version}}-full
            type=raw,value=full
      - id: build-full
        uses: docker/build-push-action@v6
        with:
          context: .
          file: docker/Dockerfile
          target: full
          platforms: ${{ matrix.platform }}
          outputs: type=image,name=${{ steps.image.outputs.name }},push-by-digest=true,name-canonical=true,push=true
          cache-from: type=gha,scope=full
          cache-to: type=gha,scope=full,mode=max
          labels: ${{ steps.meta-full.outputs.labels }}
          provenance: false
      - name: export full digest
        run: mkdir -p /tmp/digests-full && printf '%s\n' "$DIGEST" > "/tmp/digests-full/${PLATFORM//\//-}"
        env:
          DIGEST: ${{ steps.build-full.outputs.digest }}
          PLATFORM: ${{ matrix.platform }}
      - uses: actions/upload-artifact@v4
        with:
          name: digest-full-${{ strategy.job-index }}
          path: /tmp/digests-full/*
  merge:
    needs: build
    runs-on: ubuntu-latest
    permissions:
      packages: write
    steps:
      - id: image
        run: echo "name=$(echo "ghcr.io/${GITHUB_REPOSITORY}" | tr '[:upper:]' '[:lower:]')" >> "$GITHUB_OUTPUT"
      - uses: actions/download-artifact@v4
        with:
          pattern: digest-slim-*
          merge-multiple: true
          path: /tmp/digests
      - uses: actions/download-artifact@v4
        with:
          pattern: digest-full-*
          merge-multiple: true
          path: /tmp/digests-full
      - uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}
      - uses: docker/metadata-action@v5
        id: meta
        with:
          images: ${{ steps.image.outputs.name }}
          tags: |
            type=semver,pattern={{version}}
            type=semver,pattern={{major}}.{{minor}}
            type=raw,value=latest
      - id: meta-full
        uses: docker/metadata-action@v5
        with:
          images: ${{ steps.image.outputs.name }}
          tags: |
            type=semver,pattern={{version}}-full
            type=raw,value=full
      - name: create manifest
        run: |
          docker buildx imagetools create $(jq -cr '.tags | map("-t " + .) | join(" ")' <<< "$SLIM_TAGS") \
            $(printf '${{ steps.image.outputs.name }}@%s ' $(cat /tmp/digests/*))
          docker buildx imagetools create $(jq -cr '.tags | map("-t " + .) | join(" ")' <<< "$FULL_TAGS") \
            $(printf '${{ steps.image.outputs.name }}@%s ' $(cat /tmp/digests-full/*))
        env:
          SLIM_TAGS: ${{ steps.meta.outputs.json }}
          FULL_TAGS: ${{ steps.meta-full.outputs.json }}
